{
  "name": "serverless-nodejs-example",
  "version": "1.0.0",
  "description": "demo",
  "private": true,
  "dependencies": {
    "serverless-fun": "^0.0.1-alpha-1",
    "typescript": "^4.3.5"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "dev": "set mode=development && tsc --build tsconfig.json && serverless offline start",
    "build": "set mode=production && tsc --build tsconfig.json",
    "package": "set mode=production && tsc --build tsconfig.json && serverless package",
    "deploy": "set mode=production  && tsc --build tsconfig.json && serverless deploy",
    "remove": "serverless remove"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "Hansin1997",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.3.2",
    "serverless-kubeless-offline-win": "^0.1.9"
  }
}
